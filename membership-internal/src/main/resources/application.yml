server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/membership_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: members
    password: Huiyuanka123!!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  file:
    name: /Users/vincentmiao/Documents/trae_projects/members/membership-system/log/internal-backend.log
  level:
    root: INFO
    com.membership: DEBUG

# JWT配置
jwt:
  secret: your-256-bit-secret-key-for-internal-system-must-be-at-least-32-characters
  expiration: 86400000

# 文件上传路径
upload:
  path: /Users/vincentmiao/Documents/trae_projects/members/membership-system/backend/uploads